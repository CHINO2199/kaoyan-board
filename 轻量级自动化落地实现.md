# 推荐的轻量级自动化落地实现

> 用途：把 `data.json` 明文数据加密为前端可解密的 `data.enc`。
> 依赖：`pip install pycryptodome`

---

## 脚本：`update.py`（放在你的私有或公有代码仓库中）

```python
import json
import base64
from Crypto.Cipher import AES
from Crypto.Util.Padding import pad

SECRET_KEY = "YOUR_MASTER_PASSWORD_HERE".ljust(32)[:32].encode('utf-8')  # 32字节密钥


def encrypt_data(json_file_path="data.json", output_path="data.enc"):
    with open(json_file_path, "r", encoding="utf-8") as f:
        raw_text = f.read().encode('utf-8')

    # 使用 AES-CBC 或 AES-GCM 加密
    iv = b"1234567890123456"  # 16字节 IV
    cipher = AES.new(SECRET_KEY, AES.MODE_CBC, iv)
    encrypted = cipher.encrypt(pad(raw_text, AES.block_size))

    with open(output_path, "w", encoding="utf-8") as f:
        f.write(base64.b64encode(iv + encrypted).decode('utf-8'))
    print("加密完成，已更新 data.enc！")


if __name__ == "__main__":
    encrypt_data()
```

---

## 与前端约定（对应 `网站构建提示词.md`）

- 密钥：32 字节，由主密码 `ljust(32)[:32]` 补齐/截断而来。
- 密文格式：`base64(IV + AES-CBC 密文)`，IV 固定 16 字节并前置在密文中。
- 前端使用 CryptoJS 以相同方式还原：从 base64 解出前 16 字节作 IV，其余作密文，AES-256-CBC 解密后再去掉 PKCS7 padding，得到 JSON 字符串。

## 日常使用流程

```bash
# 1. 编辑明文数据
vim data.json

# 2. 重新加密
python update.py

# 3. 提交发布
git add data.enc && git commit -m "log: update 2026-09-10" && git push
```
