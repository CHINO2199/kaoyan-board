# 网站构建提示词

> 用途：交给 AI 编程助手（Cursor / Claude Code / WorkBuddy 等），生成考研百日倒计时看板的单文件前端页面。
> 产物：`index.html`（Single File，托管于 GitHub Pages）

---

## Role

顶级全栈开发工程师 & 资深 UI/UX 设计师

## 任务目标

请为我开发一个高颜值、极简克制的"考研百日倒计时与每日学习看板"前端网页。项目将托管在 GitHub Pages 上，采用纯前端单文件（Single File `index.html`）设计，结合 Tailwind CSS (CDN)、Vue 3 (CDN) 和 ECharts (CDN)。

---

## 核心设计规范与视觉要求

1. **风格意境**：Linear / Apple 风格，提供深浅双色主题（深色模式为主，搭配磨砂玻璃微渐变、精致边框和柔和阴影，营造沉浸、专注的冲刺氛围）。
2. **动效细节**：数字滚动渐显、倒计时呼吸灯效果、平滑的卡片过渡动画。

---

## 页面功能模块

### 1. 全局锁屏/解密弹窗 (Encryption Gate)

- 网站默认只读取外部的 `data.enc`（加密文本）。
- 首次访问时弹出精致的密码解锁弹层，使用 CryptoJS (AES-256) 解密。
- 解密成功后将密钥缓存至 Session/LocalStorage，免去每次刷新的输入。密码错误给出细腻的抖动反馈。

### 2. 冲刺 Hero 区域

- 醒目的"考研百日倒计时"（天、时、分、秒动态跳动）。
- 每日一句走心励志格言（或从解密数据中动态读取）。

### 3. 阶段里程碑/目标截止日 (Milestones & Deadlines)

- 卡片式展示近期的关键任务节点（例如："数学真题二刷完成 - 剩 12 天"、"政治背诵手册第一遍 - 剩 25 天"）。
- 包含进度条和距离截止日的微型倒数标签。

### 4. 每日看板与圆盘统计图 (Daily Analytics)

- **日期切换器**：可切换回看过去每一天的记录。
- **核心图表**：使用 ECharts 绘制**环形圆盘图（Donut Chart）**，展示各科学习时长占比（如政治、英语、数学、专业课），圆心显示"今日总时长（如 10.5h）"。
- **工作内容与复盘清单**：
  - 已完成的核心任务（Checklist 样式）。
  - 今日复盘/心得体会/卡点记录（支持 Markdown 简单渲染）。
  - 明日核心计划。

---

## 技术栈与实现细节

- **Single File**：HTML + Vue 3 (CDN) + Tailwind CSS (CDN) + ECharts (CDN) + CryptoJS (CDN)。
- **数据结构约定**：定义一份解密后的 JSON 示范数据模板（包含 `milestones`、`daily_logs` 数组）。
- **健壮性**：如果 `data.enc` 不存在，提供优雅的 Empty State 占位，并允许在控制台或本地临时切换为 Demo 数据调试。

请给出完整的、开箱即用的 `index.html` 源码，代码需注释清晰，注重排版美学。
